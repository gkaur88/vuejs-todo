package utility;

import java.time.Duration;

public class Util {

    public static long PAGE_LOAD_TIMEOUT = 1;
    public static long IMPLICIT_WAIT = 1;
    public static long POLLING = 100;
    public static Duration EXPLICIT_WAIT = Duration.ofSeconds(15);
}
